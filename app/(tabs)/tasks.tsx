import { Text, View } from "react-native";

export default function TasksTabScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Tasks screen coming next.</Text>
    </View>
  );
}
