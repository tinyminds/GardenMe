import { Text, View } from "react-native";

export default function SettingsTabScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Settings screen coming next.</Text>
    </View>
  );
}
